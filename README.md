# Sage10 Base Theme for Development

A base theme for development in individual use.

## Theme installation

Place this theme on WordPress with Sage installed.

```
$ composer install
$ npm install
```